# Absenteísmo v2

Cópia de trabalho criada a partir da pasta `absenteismo` (versão preservada).

| Item | Valor |
|------|--------|
| Origem | `../absenteismo` |
| Criado em | 2026-05-22 |
| Propósito | Evolução (IA, API, Radar, etc.) sem alterar a v1 |

## Como rodar

```bash
cd Absenteismov2
npm install
npm run dev
```

## Notas

- `node_modules` e `dist` não foram copiados — execute `npm install` na primeira vez.
- A pasta `absenteismo` permanece intacta como referência estável.
