# Absenteísmo V4

Cópia de trabalho criada a partir de **AbsenteismoV3** em 2026-06-04.

## O que foi copiado

- Código-fonte (`src/`, configs, `public/`, etc.)
- Documentação e scripts do projeto

## O que não foi copiado (recriar localmente)

- `node_modules/` — execute `npm install`
- `dist/` — gere com `npm run build`
- `backups/` — histórico de backups da V3
- `.git/` — repositório Git da V3 (inicie um novo `git init` aqui se quiser versionar só a V4)

## Comandos úteis

```bash
npm install
npm run dev
npm test
```
